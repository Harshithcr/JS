const accId =6294;
let email="harshith@gmail.com";
// Prefer not use var because of functional scope
var accPass="1234";

// We can declare the variable without the datatype.But avoid doing it
accCity="Bng";

// The value will be undefined whe no value is initialized 
let area;
console.table([accId,email,accPass,accCity,area]);

